spring:
  application:
    name: store

  jwt:
    secret: ${JWT_SECRET}
    access-token-expiration: 7200 #2h     #300 # 5m
    refresh-token-expiration: 604800 # 7d

  profiles:
    active: dev
stripe:
  secretKey: ${STRIPE_SECRET_KEY}
  webhookSecretKey: ${STRIPE_WEBHOOK_SECRET_KEY}